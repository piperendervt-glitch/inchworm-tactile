# 初回検証（2026-09-09）

- Python 3.12.10 / Windows、Tkinterで起動。
- `python -m unittest -v`：5件成功（再現性と移動、センサ遅延、局所性、CSV因果対応、摂食・害・死亡）。
- GUI：両方表示、触覚のみ＋縦配置、CSV記録・終了を実行。`visualizer.png` は実際のウィンドウのキャプチャ。
- `python train.py --generations 4 --population 6 --seconds 45`：探索24候補。HP時間積分は初期3954.999 → 4194.706。
- 未使用ノイズseed 101/202：初期3954.999 → 4194.679（約6.1%改善）。環境配置は同じ。
- `training-example.json` に重み、設定、探索履歴を保存。アプリの「重みを読む」で読み込める。

評価対象はこの簡易物理と初期配置だけ。未知環境の自律探索、確実な障害物回避、実機転用は未検証。
センサ遅延や局所性の自動検証は、人間による触覚識別・歩行の受け入れ判定を代替しない。
